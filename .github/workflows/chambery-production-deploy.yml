name: Deploy on Production - Chambéry 🚀

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    uses: ./.github/workflows/deploy-production.yml
    with:
      environment: production-chambery
      deployment_label: Production - Chambéry
      clever_alias: web-prod-chambery
      last_deployed_variable: CHAMBERY_LAST_DEPLOYED_SHA
      failure_logs_url: ${{ vars.CHAMBERY_FAILURE_LOGS_URL }}
    secrets:
      clever_token: ${{ secrets.CHAMBERY_CLEVER_TOKEN }}
      clever_secret: ${{ secrets.CHAMBERY_CLEVER_SECRET }}
      slack_webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
