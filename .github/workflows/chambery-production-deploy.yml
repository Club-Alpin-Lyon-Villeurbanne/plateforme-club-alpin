name: Deploy on Production - Chambéry 🚀

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    uses: ./.github/workflows/deploy-production.yml
    with:
      environment: production-chambery
      deployment_label: Production - Chambéry
      clever_alias: web-prod-chambery
      last_deployed_variable: LAST_DEPLOYED_SHA_CHAMBERY
    secrets:
      clever_token: ${{ secrets.CLEVER_TOKEN_CHAMBERY }}
      clever_secret: ${{ secrets.CLEVER_SECRET_CHAMBERY }}
      slack_webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
      last_deployed_sha: ${{ vars.LAST_DEPLOYED_SHA_CHAMBERY }}
