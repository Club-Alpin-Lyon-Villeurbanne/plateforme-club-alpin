twig:
    default_path: '%kernel.project_dir%/templates'
    globals:
        url_site: '%env(ROUTER_CONTEXT_SCHEME)%://%env(ROUTER_CONTEXT_HOST)%'
        sitename: 'Club Alpin Français de Lyon-Villeurbanne'
        recaptcha:
            site_key: "%env(RECAPTCHA_KEY)%"
        analytics_account: "%env(ANALYTICS_ACCOUNT)%"
        google_site_verification: "%env(GOOGLE_SITE_VERIFICATION)%"
        display_banner:  "%env(bool:DISPLAY_BANNER)%"

when@test:
    twig:
        strict_variables: true
