{% extends format == 'html' ? 'email/transactional/layout.html.twig' : 'email/transactional/layout.txt.twig' %}

{% block subject -%}
    Votre sortie a été refusée
{%- endblock %}

{% block inner_html %}
    <p>
        Désolé, il semble que votre sortie <a href="{{ event_url }}">{{ event_name }}</a> ne soit pas validée par les responsables. Voici ci-dessous le message joint :
    </p>
    <p>
        {{ message | converturls | nl2br }}
    </p>
{% endblock %}

{% block inner_txt %}
Désolé, il semble que votre sortie {{ event_name }} ({{ event_url }}) ne soit pas validée par les responsables. Voici ci-dessous le message joint :

    {{ message }}
{% endblock %}
