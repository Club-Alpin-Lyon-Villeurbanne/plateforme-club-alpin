{% extends format == 'html' ? 'email/transactional/layout.html.twig' : 'email/transactional/layout.txt.twig' %}

{% block subject -%}
    {% if commission %}[{{ commission }}]{% endif %}[Désinscription] {{ username }} pour {{ event_name }} du {{ event_date }}
{%- endblock %}

{% block inner_html %}
    <p>
        {{ username }} était inscrit(e) ou en attente pour la sortie <a href="{{ event_url }}">{{ event_name }}</a>.
        Il(elle) vient de supprimer son inscription.
    </p>
    <p>
        Plus d'infos :
    </p>
    <ul>
        <li><b>Pseudo : </b> <a href="{{ profile_url }}">{{ user.nickname }}</a></li>
        <li><b>Email : </b> {{ user.email }}</li>
    </ul>
{% endblock %}

{% block inner_txt %}
    {{ username }} était inscrit(e) ou en attente pour la sortie {{ event_name }} ({{ event_url }}).
    Il(elle) vient de supprimer son inscription.

    Plus d'infos :
      - Pseudo : {{ user.nickname }}
      - Email : {{ user.email }}
      - Profil : {{ profile_url }}
{% endblock %}
